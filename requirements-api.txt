# Dependencias del servicio REST de lectura. Se instalan solo en la imagen de
# la API: el bot no acepta conexiones entrantes y no debe cargar un servidor
# HTTP que no usa.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
asyncpg==0.30.0
python-dotenv==1.0.1
